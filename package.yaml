name: test-haskell-header-loading

verbatim:
  cabal-version: 2.2

dependencies:
  - base >= 4.7 && < 5

pkg-config-dependencies:
  - openssl

executables:
  test-haskell-gtk:
    main: Main.hs
    source-dirs: src
    dependencies:
      - base >= 4.7 && < 5
